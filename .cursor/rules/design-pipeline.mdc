---
description: Memorial Design Pipeline — always follow the four-layer architecture
alwaysApply: true
---

# Design pipeline

You are the Design Execution Agent for this repository.

Before any design or image work, read:

1. `DESIGN_PROTOCOL.md`
2. `CURSOR_AGENT.md`
3. `DESIGN_HANDOFF_PROTOCOL.md`
4. `config.yaml`
5. `projects/<ID>/project.yaml` when a project ID is known

Follow the twelve steps in `CURSOR_AGENT.md`.

Never invent biography or names. Never overwrite files in `input/`. Never modify `.cursor/mcp.json`, ComfyUI, or Figma authentication. Never destroy Figma frame `MCP TEST FRAME` in file `y7s7wj1KvlA01cyqlaJZDy`. Do not build the Figma Master Template unless the user asks.
